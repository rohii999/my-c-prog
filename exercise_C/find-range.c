#include<stdio.h>
int main(){

    int a,b;
    int i;

    printf("enter the no:");
    scanf("%d%d",&a,&b);

    
  
        if(a,b>=100 && a,b<=200){
           printf("the given value is in range100...200:%d%d",a,b);
        }
        else{
            printf("nthg");
        } 
}